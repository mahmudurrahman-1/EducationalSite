package driver;

import org.openqa.selenium.WebDriver;

public class PageDriver {
    //threadlocal 
    private static final ThreadLocal<WebDriver> webdriver =new ThreadLocal<>();
    private static PageDriver
}
